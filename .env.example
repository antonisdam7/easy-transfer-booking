PORT=4000
JWT_SECRET=change-me-super-secret
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
DATABASE_URL=
DATABASE_SSL=true

# Email (use Resend on Render free tier — SMTP ports 587/465 are blocked)
RESEND_API_KEY=re_xxxxxxxx
MAIL_FROM=bookings@yourdomain.com
MAIL_TO=habibitransferscrete@gmail.com
# EMAIL_PROVIDER=auto

# Optional: SMTP for local dev or paid Render (blocked on Render free tier)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
